// Enums are used with Object.freeze({})
// -> Why we use Object.freeze({}) : Because if we want to add any property in the Enum out of the object.freeze enumm, so object.freeze stop to add/define the new property.
// -> If want to add new property so you need to add that new property inside the object.freeze of enum. 